# Variables d'environnement pour WalkiWorki

# IP locale autorisée (côté serveur uniquement - ne pas préfixer avec VITE_)
LOCAL_IP=127.0.0.1

# Autres IPs autorisées (optionnel)
# ALLOWED_IP_1=192.168.1.100
# ALLOWED_IP_2=10.0.0.50

# Variables publiques (préfixées avec VITE_)
VITE_NEWS_API_TOKEN=your_news_api_token_here 